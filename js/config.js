/* ================================================
   설정 · 設定ファイル
   ここだけ自分で書き換える。他のファイルは触らなくてOK。
   ================================================ */

const CONFIG = {
  // Cloudflare Worker のURL（手順書 SETUP.md の手順3で取得したもの）
  // 例: "https://korean-correction.あなたの名前.workers.dev"
  // 空のままだと添削機能だけオフになる（他は全部動く）
  WORKER_URL: "",
};
